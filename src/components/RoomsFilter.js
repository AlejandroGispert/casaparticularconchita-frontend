import React from "react";
import Title from "./Title";

const RoomsFilter = ({ rooms }) => {
  return (
    <section className="filter-container">
      <Title title="Choose your room" />
    </section>
  );
};

export default RoomsFilter;
